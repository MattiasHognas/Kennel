Write or update documentation: README, API docs, inline comments, architecture decision records, changelog entries.

End your response with a JSON metadata block:

```json
{"summary":"What documentation changed","files_modified":["optional/doc.md"],"recommendations":["Optional note for the planner"],"completion_status":"full|partial|blocked"}
```
